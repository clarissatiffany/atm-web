function withdraw(username, session, amount){
    // ambil sekian dari username tersebut
    // jangan lupa cek session (pake storage.js)
}

function deposit(username, session, amount){
    // deposit sekian dari username tersebut
    // karena ini sandbox, langsung update aja
    // jangan lupa cek session (pake storage.js)
}

function transfer(username1, username2, session, amount){
    // transfer dari username1 ke username2 KALAU pin-nya bener
    // cek amount juga
}